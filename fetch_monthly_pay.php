<?php
include 'database.php';

if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['consumer_id'])) {
    $consumerId = intval($_POST['consumer_id']);

    // Updated SQL query to fetch the most recent record for the current month and include status
    $sql = "
        SELECT 
            c.account_number, 
            c.first_name, 
            c.last_name, 
            em.meter_id,
            dc.energy_consumed AS recent_consumption,
            (dc.energy_consumed * 7) AS monthly_consumption,
            (dc.energy_consumed * 30 * 7) AS total_pay,
            dc.date AS bill_date,
            dc.status AS consumption_status  -- Include status column
        FROM 
            consumers c
        LEFT JOIN 
            electricitymeters em ON c.consumer_id = em.consumer_id
        LEFT JOIN 
            consumption_records dc ON c.consumer_id = dc.consumer_id
        WHERE 
            c.consumer_id = $consumerId 
        AND 
            MONTH(dc.date) = MONTH(CURDATE()) 
        AND 
            YEAR(dc.date) = YEAR(CURDATE())
        ORDER BY 
            dc.date DESC
        LIMIT 1
    ";

    $result = mysqli_query($conn, $sql);

    if ($result && $row = mysqli_fetch_assoc($result)) {
        $consumerName = htmlspecialchars($row['first_name'] . ' ' . $row['last_name']); // Combine first and last name

        echo "
        <h2>Monthly Pay Receipt</h2>
        <p><strong>Account Number:</strong> {$row['account_number']}</p>
        <p><strong>Consumer Name:</strong> {$consumerName}</p>
        <p><strong>Meter ID:</strong> {$row['meter_id']}</p>
        <p><strong>Bill Date:</strong> {$row['bill_date']}</p>
        <p><strong>Monthly Consumption:</strong> {$row['monthly_consumption']} kWh</p>
        <p><strong>Total Pay:</strong> ₱{$row['total_pay']}</p>
        <p><strong>Status:</strong> {$row['consumption_status']}</p> <!-- Display the status -->
        <p><strong>Generated By:</strong> {$consumerName}</p>
        <p><em>Date Generated:</em> " . date('Y-m-d') . "</p>
        ";
    } else {
        echo "<p>No records found for this consumer in the current month.</p>";
    }

    mysqli_close($conn);
} else {
    echo "<p>Invalid request.</p>";
}
?>
